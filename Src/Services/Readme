Services 디렉토리는 주로 앱에서 외부 API를 호출하거나 네트워크 요청을 처리하는 함수들이 위치하는 디렉토리입니다.
이 디렉토리는 앱의 비즈니스 로직과 네트워크 요청과 같은 데이터 통신을 분리하여 코드를 모듈화하는 데 도움이 됩니다.

즉, 실제 서버와 통신하기 위한 코드들이 모여 있는 곳 입니다.

1. API 호출: 외부 API 서버와 통신하기 위한 함수들이 위치합니다. 이 함수들은 API 요청을 보내고 응답을 처리하는 역할을 수행합니다.
2. 네트워크 요청: API 호출 뿐만 아니라 서버와의 기타 네트워크 요청을 처리하는 함수들이 있을 수 있습니다. 예를 들어 이미지 업로드, 파일 다운로드 등의 기능이 해당됩니다.
3. 데이터 변환 및 가공: 서버로부터 받은 데이터를 원하는 형식으로 변환하거나 가공하는 함수들이 있을 수 있습니다. 이는 서버에서 받은 데이터를 앱의 상태(State)로 적절하게 변환하는 작업에 해당됩니다.
4. 인증과 관련된 기능: 사용자 인증과 관련하여 토큰을 저장하고 검증하는 함수들이 위치할 수 있습니다.
5. 그 외 유틸리티 함수들: 네트워크 요청과 관련하여 공통으로 사용되는 유틸리티 함수들이 위치할 수 있습니다.

사용될 라이브러리 및 모듈은
Axios 등이 존재 합니다.